<h3>Bulk Discounts</h3>

<% @merchant.bulk_discounts.each do |disc| %>
<div id="discount-<%= disc.id %>">
  <p>Discount: %<%= disc.percentage %></p>
  <p>Max Quantity: <%= disc.quantity_threshold %></p>
  <%= link_to "##{disc.id} Discount Information", "/merchants/#{@merchant.id}/bulk_discounts/#{disc.id}" %>
  </div>
<% end %>
<%= link_to 'Create New Discount', new_merchant_bulk_discount_path(@merchant.id) %>